params{
    reads        = false
    key_file     = false
    gcp_id       = null
    genome_fasta = false
}

process {
    container = 'quay.io/lifebitai/lifebit-ai-fasp:latest'
}

profiles {
    docker { docker.enabled = true }
}